<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WGO" Height="920" Width="1300" MinHeight="650" MinWidth="1000" WindowStartupLocation="CenterScreen"
        Background="#FF202020" FontFamily="Segoe UI">
    <Window.Resources>
        <!-- ================= FLUENT WIN11 DARK/BLUE PALETTE ================= -->
        <SolidColorBrush x:Key="BgDark" Color="#FF1A1B1E"/>
        <SolidColorBrush x:Key="BgPanel" Color="#FF232428"/>
        <SolidColorBrush x:Key="BgCard" Color="#FF2B2D31"/>
        <SolidColorBrush x:Key="BgCardHover" Color="#FF32353A"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF4CC2FF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FF75D2FF"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#FF2AA0DE"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF3F4F6"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FF9CA3AF"/>
        <SolidColorBrush x:Key="BorderBrush1" Color="#FF3A3C41"/>
        <SolidColorBrush x:Key="BorderAccentBrush" Color="#552EA6E0"/>
        <SolidColorBrush x:Key="LogBg" Color="#FF141517"/>
        <SolidColorBrush x:Key="LogText" Color="#FF7CE0C6"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FFFF6B6B"/>
        <SolidColorBrush x:Key="WarningBorderBrush" Color="#FFFF6B6B"/>

        <DropShadowEffect x:Key="CardShadow" Color="#FF000000" Direction="270" ShadowDepth="2" BlurRadius="14" Opacity="0.35"/>

        <!-- ================= BASE TEXT ================= -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>

        <!-- ================= FLUENT TOGGLE-SWITCH CHECKBOX ================= -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Content}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="300"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="track" Grid.Column="0" Width="42" Height="22" CornerRadius="11"
                                    Background="#FF44464C" BorderBrush="#FF616469" BorderThickness="1" VerticalAlignment="Center">
                                <Ellipse x:Name="thumb" Width="15" Height="15" Fill="#FFE7E8EA" HorizontalAlignment="Left" Margin="3,0,0,0"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="track" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                                <Setter TargetName="thumb" Property="Fill" Value="#FF0B0B0B"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="track" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT RADIOBUTTON ================= -->
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse x:Name="ring" Grid.Column="0" Width="18" Height="18" Stroke="#FF616469" StrokeThickness="1.4" Fill="Transparent" VerticalAlignment="Center"/>
                            <Ellipse x:Name="dot" Grid.Column="0" Width="9" Height="9" Fill="{DynamicResource AccentBrush}" Opacity="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <ContentPresenter Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="dot" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT TEXTBOX ================= -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource BgCard}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= GROUPBOX AS FLUENT SETTINGS CARD ================= -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Background" Value="{DynamicResource BgPanel}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="18,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Effect="{StaticResource CardShadow}">
                            <StackPanel Margin="{TemplateBinding Padding}">
                                <TextBlock Text="{TemplateBinding Header}" FontSize="14" FontWeight="SemiBold"
                                           Foreground="{TemplateBinding Foreground}" Margin="0,0,0,10"/>
                                <ContentPresenter/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT ACCENT-FILL BUTTON (PRIMARY) ================= -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#FF0B0B0B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#FF43454A"/>
                                <Setter Property="Foreground" Value="#FF8A8D92"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= SECONDARY (OUTLINE) BUTTON ================= -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.4" CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1F4CC2FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#334CC2FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF6C6F75"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#FF6C6F75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT SEGMENTED TABS ================= -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border x:Name="bd" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Border x:Name="indicator" Height="3" VerticalAlignment="Bottom" Background="{DynamicResource AccentBrush}"
                                    CornerRadius="2" Opacity="0" Margin="18,0,18,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="indicator" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                <Setter TargetName="bd" Property="Background" Value="#14FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT COMBOBOX ================= -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="3,1">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#FF0B0B0B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource BgCard}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggle" Background="Transparent" BorderThickness="0" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Border x:Name="chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" IsHitTestVisible="False">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}" VerticalAlignment="Center"/>
                                    <Path Grid.Column="1" Data="M0,0 L6,6 L12,0" Stroke="{DynamicResource TextSecondary}"
                                          StrokeThickness="1.6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Fade">
                                <Border Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderBrush1}" BorderThickness="1"
                                        CornerRadius="8" Margin="0,4,0,0" Padding="2" Effect="{StaticResource CardShadow}"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ItemsPresenter/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= FLUENT CARD (App Installer items) ================= -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BgCardHover}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GridOptionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Width" Value="360"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BgCardHover}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WrapCheckBoxStyle" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Width="300"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= SLIM FLUENT SCROLLBAR ================= -->
        <Style x:Key="ScrollThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#FF56585E" CornerRadius="4" Margin="2,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollRepeatButtonStyle" TargetType="RepeatButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollRepeatButtonStyle}" Command="ScrollBar.PageUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumbStyle}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollRepeatButtonStyle}" Command="ScrollBar.PageDownCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Background="{DynamicResource BgDark}">
        <!-- HEADER -->
        <Border DockPanel.Dock="Top" Padding="18,14" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="0,0,0,2">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#FF23262B" Offset="0"/>
                    <GradientStop Color="#FF1E2530" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" CornerRadius="8" Margin="0,0,12,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF4CC2FF" Offset="0"/>
                                <GradientStop Color="#FF1A6FBF" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="W" FontSize="17" FontWeight="Bold" Foreground="#FF0B0B0B" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock x:Name="txtAppTitle" Text="WGO - Windows General Optimizations" FontSize="17" FontWeight="Bold" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>
                <Button x:Name="btnThemeToggle" Grid.Column="1" Content="Light Mode" Style="{StaticResource SecondaryButtonStyle}" Padding="12,6" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtLblLanguage" Grid.Column="2" Text="Language:" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbLanguage" Grid.Column="3" SelectedIndex="1">
                    <ComboBoxItem Content="en-US"/>
                    <ComboBoxItem Content="pt-BR"/>
                    <ComboBoxItem Content="es-ES"/>
                    <ComboBoxItem Content="zh-CN"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- LOG CONSOLE -->
        <Border DockPanel.Dock="Bottom" Background="{DynamicResource LogBg}" BorderBrush="{DynamicResource BorderBrush1}" BorderThickness="1"
                CornerRadius="10" Padding="14,10" Margin="14,0,14,14" Height="180" Effect="{StaticResource CardShadow}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <Ellipse Width="8" Height="8" Fill="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtLogHeader" Text="Execution Log" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" FontSize="13"/>
                </StackPanel>
                <ScrollViewer x:Name="scrollLog" VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="txtLog" Background="Transparent" Foreground="{DynamicResource LogText}" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                             BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"/>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- PERSISTENT ACTION BAR - dynamic based on selected tab -->
        <Border DockPanel.Dock="Bottom" Background="{DynamicResource BgCard}" BorderBrush="{DynamicResource BorderBrush1}" BorderThickness="1"
                CornerRadius="10" Padding="14,10" Margin="14,0,14,10" Effect="{StaticResource CardShadow}">
            <StackPanel>
                <!-- Optimizations tab actions -->
                <StackPanel x:Name="pnlOptimizationsActions" Orientation="Horizontal" Visibility="Visible">
                    <Button x:Name="btnRunSelected" Content="Run Selected Optimizations" Padding="20,10" FontSize="14" Margin="0,0,10,0"/>
                    <Button x:Name="btnRestoreDefaults" Content="Restore Defaults" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0"
                            ToolTip="Restores registry and service defaults for the selected category."/>
                    <TextBlock x:Name="lblRestoreCategory" Text="Category:" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="cmbRestoreCategory" Width="120" VerticalAlignment="Center" SelectedIndex="0" Margin="0,0,10,0"
                              ToolTip="Restores registry and service defaults for the selected category.">
                        <ComboBoxItem Content="All" Tag="All"/>
                        <ComboBoxItem Content="Privacy" Tag="Privacy"/>
                        <ComboBoxItem Content="Network" Tag="Network"/>
                        <ComboBoxItem Content="Services" Tag="Services"/>
                        <ComboBoxItem Content="Visual" Tag="Visual"/>
                        <ComboBoxItem Content="AMD" Tag="Amd"/>
                    </ComboBox>
                    <Button x:Name="btnExportProfile" Content="Export Profile" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                    <Button x:Name="btnImportProfile" Content="Import Profile" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0"/>
                </StackPanel>

                <!-- Installer tab actions -->
                <StackPanel x:Name="pnlInstallerActions" Orientation="Horizontal" Visibility="Collapsed">
                    <Button x:Name="btnInstallApps" Content="Install Selected" Padding="20,10" FontSize="14"/>
                </StackPanel>

                <!-- Other tabs (External Scripts, Utilities) - no actions, just empty panel -->
                <StackPanel x:Name="pnlOtherActions" Orientation="Horizontal" Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <!-- TABS -->
        <TabControl x:Name="tabMain" Background="{DynamicResource BgDark}" Margin="10" BorderThickness="0">
            <TabItem x:Name="tabOptimizations" Header="Optimizations &amp; Privacy">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">

                        <CheckBox x:Name="chkSelectAll" Content="Select All / Deselect All" FontWeight="Bold" Margin="0,0,0,4" IsChecked="True"/>
                        <CheckBox x:Name="chkDryRun" Content="Dry Run (only log what would change, apply nothing)" Margin="0,0,0,10" IsChecked="False"/>

                        <GroupBox x:Name="grpProfiles" Header="Predefined Profiles">
                            <WrapPanel>
                                <Button x:Name="btnProfileBasic"   Content="Basic Optimization"   Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                                <Button x:Name="btnProfileLaptop"  Content="Laptop Optimization"  Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                                <Button x:Name="btnProfileGamer"   Content="Gamer PC Optimization" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                                <Button x:Name="btnProfilePrivacy" Content="Maximum Privacy"        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                                <Button x:Name="btnProfileEsports" Content="eSports"                Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                                <Button x:Name="btnProfileMax"     Content="Maximum Optimization" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,10" Padding="14,8"/>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpRestore" Header="System Safety">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnCreateRestore" Content="Create Restore Point" Style="{StaticResource SecondaryButtonStyle}"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpBloat" Header="Bloatware Removal">
                            <CheckBox x:Name="chkBloat" Content="Remove bloatware / AI apps" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpSearch" Header="Start Menu Search">
                            <CheckBox x:Name="chkSearch" Content="Force 100% local search" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpVisual" Header="Visual Effects">
                            <CheckBox x:Name="chkVisual" Content="Apply custom performance visual effects profile" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpPrivacy" Header="Deep Privacy / Telemetry">
                            <CheckBox x:Name="chkPrivacy" Content="Block telemetry / WER / CEIP / Activity Feed / Location" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpDrivers" Header="Windows Update Drivers">
                            <CheckBox x:Name="chkDrivers" Content="Block automatic driver installation" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpPagefile" Header="Virtual Memory (Pagefile)">
                            <CheckBox x:Name="chkPagefile" Content="Set static pagefile size (RAM x 1.5)" IsChecked="True"/>
                        </GroupBox>

                        <GroupBox x:Name="grpExtraPrivacy" Header="Additional Telemetry Blocking">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkAdvertisingId" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable advertising ID" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkTailoredExp" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable tailored experiences / suggestions" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDiagTrackSvc" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable DiagTrack / dmwappushservice" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkCopilotBlock" Style="{StaticResource WrapCheckBoxStyle}" Content="Block Copilot / Recall / AI data analysis" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkInputTelemetry" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable input personalization / clipboard cloud sync" IsChecked="True"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAdvancedTweaks" Header="Advanced System Tweaks">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkEdgeWidgets" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Edge preload and Widgets" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDeliveryOpt" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Delivery Optimization (P2P)" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkAppsBackground" Style="{StaticResource WrapCheckBoxStyle}" Content="Suspend UWP apps in background" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkNetworkLatency" Style="{StaticResource WrapCheckBoxStyle}" Content="Network optimization / latency reduction (TCP-IP)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkHungAppTimeout" Style="{StaticResource WrapCheckBoxStyle}" Content="Reduce hung app timeout (faster response when a program freezes)" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpServiceMgmt" Header="Service Management &amp; Advanced Cleanup">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableSysMain" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable SysMain (SuperFetch) - frees RAM/CPU, recommended for SSDs" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableWSearch" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Search (WSearch) - stops file indexing (breaks Start Menu search)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableSpooler" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Print Spooler (Spooler) - only if you don't use a printer" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkWinSxSCleanup" Style="{StaticResource WrapCheckBoxStyle}" Content="Clean WinSxS component store (frees disk space, can be time-consuming)" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <Border BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1.5" CornerRadius="10" Margin="0,10,0,0" Padding="14,10">
                            <StackPanel>
                                <TextBlock x:Name="txtXboxServicesNote" Text="Kept separate from every profile and from Select All: Game Pass and Xbox achievements can depend on these services. Only enable this if you don't play any Xbox app or Microsoft Store games." TextWrapping="Wrap" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,0,10" FontSize="12"/>
                                <GroupBox x:Name="grpXboxServices" Header="Xbox Services (not included in any profile)" BorderBrush="{DynamicResource AccentBrush}">
                                    <WrapPanel>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkDisableXboxServices" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Xbox services (XblAuthManager, XblGameSave, XboxNetApiSvc, XboxGipSvc)" IsChecked="False"/>
                                        </Border>
                                    </WrapPanel>
                                </GroupBox>
                            </StackPanel>
                        </Border>

                        <GroupBox x:Name="grpMoreOptimizations" Header="System Cleanup &amp; Performance">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkHibernation" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Hibernation" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkPowerPlan" Style="{StaticResource WrapCheckBoxStyle}" Content="Set power plan to High Performance" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkTempCleanup" Style="{StaticResource WrapCheckBoxStyle}" Content="Clean temp files / Prefetch / Windows.old / WU cache" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkHotCorners" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Snap Assist / Aero Shake" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkBootTimeout" Style="{StaticResource WrapCheckBoxStyle}" Content="Reduce boot menu timeout to 5s" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkOfficeTelemetry" Style="{StaticResource WrapCheckBoxStyle}" Content="Block Office / OneDrive telemetry" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkExtraSchedTasks" Style="{StaticResource WrapCheckBoxStyle}" Content="Remove additional telemetry scheduled tasks" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDiskOptimize" Style="{StaticResource WrapCheckBoxStyle}" Content="Auto-configure TRIM / scheduled defrag (SSD/HDD)" IsChecked="True"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkHagsGameMode" Style="{StaticResource WrapCheckBoxStyle}" Content="Enable Game Mode and HAGS" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkUltimatePerf" Style="{StaticResource WrapCheckBoxStyle}" Content="Enable Ultimate Performance with CPU min state at 100%" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkKernelGamingPriority" Style="{StaticResource WrapCheckBoxStyle}" Content="Prioritize foreground games (kernel + MMCSS)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkGameDvrDisable" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Xbox Game DVR background recording" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkGameBarMicFix" Style="{StaticResource WrapCheckBoxStyle}" Content="Fix Game Bar microphone (disables echo cancellation, fixes robotic/distorted mic audio)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkInputLagReduction" Style="{StaticResource WrapCheckBoxStyle}" Content="Reduce input lag (mouse accel, Sticky Keys, Fullscreen Optimizations)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkSearchIndexOptimize" Style="{StaticResource WrapCheckBoxStyle}" Content="Exclude junk/cache folders from Windows Search indexing" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkGhostAdapters" Style="{StaticResource WrapCheckBoxStyle}" Content="Remove hidden/ghost network adapters (Device Manager)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkFastStartup" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Fast Startup (prevents accumulated RAM leak on boot)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkResidualServices" Style="{StaticResource WrapCheckBoxStyle}" Content="Set low-value background services (PcaSvc, WerSvc, wisvc, RetailDemo) to Manual" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkStandbyListClean" Style="{StaticResource WrapCheckBoxStyle}" Content="Clear Standby List memory cache now (frees cached RAM instantly)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkLargeSystemCache" Style="{StaticResource WrapCheckBoxStyle}" Content="Optimize system cache for high-RAM systems (8GB+, auto-detected)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkAutoStandbyClean" Style="{StaticResource WrapCheckBoxStyle}" Content="Auto-clean Standby List every 5 minutes (background scheduled task)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkClearEventLogs" Style="{StaticResource WrapCheckBoxStyle}" Content="Clear Windows Event Logs" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDeleteMinidump" Style="{StaticResource WrapCheckBoxStyle}" Content="Delete Minidump Files" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkClearStoreCache" Style="{StaticResource WrapCheckBoxStyle}" Content="Clear Store Cache" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpCpuTimerTweaks" Header="CPU &amp; Timer Tweaks">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableCoreParking" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Core Parking (keeps all CPU cores active, reduces latency in games)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableHPET" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable HPET - may improve FPS on some systems (requires restart)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkTimerResolution" Style="{StaticResource WrapCheckBoxStyle}" Content="Increase timer resolution to 0.5ms (reduces input lag)" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpGpuTweaks" Header="GPU &amp; Driver Tweaks">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkIncreaseTdrNvidia" Style="{StaticResource WrapCheckBoxStyle}" Content="Increase NVIDIA driver timeout (TDR) to prevent crashes in demanding games" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableNvidiaTelemetry" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable NVIDIA telemetry services and scheduled tasks" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpNetworkAdvanced" Header="Advanced Network Tweaks">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableNagle" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Nagle's algorithm on all network adapters" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableIPv6" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable IPv6 on all network interfaces" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkRssOptimize" Style="{StaticResource WrapCheckBoxStyle}" Content="Optimize Receive Side Scaling (RSS) for lower network latency" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpExtraTweaks2" Header="Additional Privacy &amp; Cleanup">
                            <WrapPanel>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkHostsBlock" Style="{StaticResource WrapCheckBoxStyle}" Content="Block telemetry domains via hosts file" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkPrivacyDeep" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Shared Experiences and Cortana completely" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkCacheClean" Style="{StaticResource WrapCheckBoxStyle}" Content="Clear icon and font cache" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkUiCleanup" Style="{StaticResource WrapCheckBoxStyle}" Content="Remove People bar, News &amp; Interests icon, Ink Workspace" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkTcpAutotuning" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable TCP window autotuning" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDoH" Style="{StaticResource WrapCheckBoxStyle}" Content="Set Cloudflare DNS and enable DNS over HTTPS" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkFastShutdown" Style="{StaticResource WrapCheckBoxStyle}" Content="Skip pagefile clearing on shutdown (faster shutdown)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkPrefetchSSD" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Prefetch on SSDs (auto-detected)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkRemoveWinBackup" Style="{StaticResource WrapCheckBoxStyle}" Content="Silence 'Windows Backup' nag notifications (it's a locked system component - Windows won't let it be uninstalled or hidden from Settings)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkTcpIpReset" Style="{StaticResource WrapCheckBoxStyle}" Content="Reset TCP/IP stack (fixes network connectivity issues)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkRemoveOnedrive" Style="{StaticResource WrapCheckBoxStyle}" Content="Completely uninstall OneDrive from the system" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableGameBar" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable the Game Bar interface and shortcuts" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableStore" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Store (prevents background activity, keeps winget)" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableWer" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Error Reporting completely" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkPauseUpdates" Style="{StaticResource WrapCheckBoxStyle}" Content="Pause Updates for 7 Days" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableEdgeTelemetry" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Edge Telemetry" IsChecked="False"/>
                                </Border>
                                <Border Style="{StaticResource GridOptionCardStyle}">
                                    <CheckBox x:Name="chkDisableSpotlight" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Spotlight" IsChecked="False"/>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <Border BorderBrush="{DynamicResource WarningBorderBrush}" BorderThickness="1.5" CornerRadius="10" Margin="0,10,0,0" Padding="14,10">
                            <StackPanel>
                                <TextBlock x:Name="txtRiskyWarning" Text="Warning: the options below weaken Windows security or update mechanisms. Only enable them if you understand and accept the trade-off." TextWrapping="Wrap" Foreground="{DynamicResource WarningBrush}" FontWeight="Bold" Margin="0,0,0,10" FontSize="12"/>
                                <GroupBox x:Name="grpRiskyTweaks" Header="Risky Tweaks (advanced)" BorderBrush="{DynamicResource WarningBorderBrush}">
                                    <WrapPanel>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyUAC" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable User Account Control (UAC)" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskySmartScreen" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable SmartScreen (Windows and Edge)" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyDefenderRT" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Defender real-time protection" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyWinUpdateSvc" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable the Windows Update service" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyBits" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable BITS (Background Intelligent Transfer)" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyDisableFirewall" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable Windows Firewall (high risk, offline games only)" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyDisableDEP" Style="{StaticResource WrapCheckBoxStyle}" Content="Disable DEP (Data Execution Prevention) - may cause instability" IsChecked="False"/>
                                        </Border>
                                        <Border Style="{StaticResource GridOptionCardStyle}">
                                            <CheckBox x:Name="chkRiskyNvidiaMaxPerf" Style="{StaticResource WrapCheckBoxStyle}" Content="Force NVIDIA GPU maximum performance mode (may increase power consumption)" IsChecked="False"/>
                                        </Border>
                                    </WrapPanel>
                                </GroupBox>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="tabAmdGpu" Header="AMD Fix">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock x:Name="txtAmdGpuBanner" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" FontStyle="Italic" Margin="0,0,0,10" FontSize="12"/>
                        <GroupBox x:Name="grpAmdGpu" Header="AMD Radeon Stability Fixes">
                            <StackPanel>
                                <CheckBox x:Name="chkAmdUlps" Content="Disable ULPS"/>
                                <CheckBox x:Name="chkAmdMpo" Content="Disable MPO"/>
                                <CheckBox x:Name="chkAmdTdr" Content="Extend TDR Delay"/>
                                <CheckBox x:Name="chkAmdCrashDefender" Content="Disable AMD Crash Defender"/>
                                <CheckBox x:Name="chkAmdHdcp" Content="Disable HDCP"/>
                                <CheckBox x:Name="chkAmdTelemetry" Content="Disable AMD Telemetry"/>
                                <CheckBox x:Name="chkAmdHwAccel" Content="Fix Browser/Electron Hardware Acceleration"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="tabInstaller" Header="App Installer">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="txtScoopRequired" Text="Apps install via winget, falling back to Scoop when needed." TextWrapping="Wrap" FontWeight="Bold"/>
                                <TextBlock x:Name="txtScoopStatus" Text="" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,2,0,6" FontSize="12"/>
                                <Button x:Name="btnInstallScoop" Content="Install Scoop" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                            </StackPanel>
                        </Border>

                        <GroupBox x:Name="grpAppsBrowsers" Header="Web Browsers">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkFirefox" Content="Mozilla Firefox" FontWeight="Bold" Tag="firefox"/>
                                        <TextBlock x:Name="txtFirefoxDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkBrave" Content="Brave" FontWeight="Bold" Tag="brave"/>
                                        <TextBlock x:Name="txtBraveDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAppsFiles" Header="File &amp; Archive Tools">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkNanaZip" Content="NanaZip" FontWeight="Bold" Tag="nanazip"/>
                                        <TextBlock x:Name="txtNanaZipDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkSevenZip" Content="7-Zip" FontWeight="Bold" Tag="7zip"/>
                                        <TextBlock x:Name="txtSevenZipDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkNpp" Content="Notepad++" FontWeight="Bold" Tag="notepadplusplus.install"/>
                                        <TextBlock x:Name="txtNppDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkWiztree" Content="WizTree" FontWeight="Bold" Tag="wiztree"/>
                                        <TextBlock x:Name="txtWiztreeDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAppsDownloads" Header="Downloads &amp; Torrents">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkFdm" Content="Free Download Manager" FontWeight="Bold" Tag="freedownloadmanager"/>
                                        <TextBlock x:Name="txtFdmDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkQbt" Content="qBittorrent" FontWeight="Bold" Tag="qbittorrent"/>
                                        <TextBlock x:Name="txtQbtDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAppsGaming" Header="Gaming &amp; Streaming">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkSteam" Content="Steam" FontWeight="Bold" Tag="steam"/>
                                        <TextBlock x:Name="txtSteamDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkEpic" Content="Epic Games Launcher" FontWeight="Bold" Tag="epicgameslauncher"/>
                                        <TextBlock x:Name="txtEpicDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkGog" Content="GOG Galaxy" FontWeight="Bold" Tag="goggalaxy"/>
                                        <TextBlock x:Name="txtGogDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkMoonlight" Content="Moonlight" FontWeight="Bold" Tag="moonlight"/>
                                        <TextBlock x:Name="txtMoonlightDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkSunshine" Content="Sunshine" FontWeight="Bold" Tag="sunshine"/>
                                        <TextBlock x:Name="txtSunshineDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAppsMonitoring" Header="System Monitoring &amp; Cleanup">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkCpuz" Content="CPU-Z" FontWeight="Bold" Tag="cpuz"/>
                                        <TextBlock x:Name="txtCpuzDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkHwinfo" Content="HWiNFO" FontWeight="Bold" Tag="hwinfo"/>
                                        <TextBlock x:Name="txtHwinfoDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkMemreduct" Content="Mem Reduct" FontWeight="Bold" Tag="memreduct"/>
                                        <TextBlock x:Name="txtMemreductDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkBleachbit" Content="BleachBit" FontWeight="Bold" Tag="bleachbit"/>
                                        <TextBlock x:Name="txtBleachbitDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkDnsJumper" Content="DNS Jumper" FontWeight="Bold" Tag="dnsjumper"/>
                                        <TextBlock x:Name="txtDnsJumperDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkCapFrameX" Content="CapFrameX" FontWeight="Bold" Tag="capframex"/>
                                        <TextBlock x:Name="txtCapFrameXDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkMsiAfterburner" Content="MSI Afterburner" FontWeight="Bold" Tag="msiafterburner"/>
                                        <TextBlock x:Name="txtMsiAfterburnerDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkRtss" Content="RivaTuner Statistics Server" FontWeight="Bold" Tag="rtss"/>
                                        <TextBlock x:Name="txtRtssDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkDlssSwapper" Content="DLSS Swapper" FontWeight="Bold" Tag="dlssswapper"/>
                                        <TextBlock x:Name="txtDlssSwapperDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkDdu" Content="Display Driver Uninstaller" FontWeight="Bold" Tag="ddu"/>
                                        <TextBlock x:Name="txtDduDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkHwMonitor" Content="HWMonitor" FontWeight="Bold" Tag="hwmonitor"/>
                                        <TextBlock x:Name="txtHwMonitorDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAppsProductivity" Header="Productivity &amp; Customization">
                            <WrapPanel>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkFlowLauncher" Content="Flow Launcher" FontWeight="Bold" Tag="flowlauncher"/>
                                        <TextBlock x:Name="txtFlowLauncherDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkNilesoftShell" Content="Nilesoft Shell" FontWeight="Bold" Tag="nilesoftshell"/>
                                        <TextBlock x:Name="txtNilesoftShellDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <CheckBox x:Name="chkShareX" Content="ShareX" FontWeight="Bold" Tag="sharex"/>
                                        <TextBlock x:Name="txtShareXDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="24,2,0,0" FontSize="12"/>
                                    </StackPanel>
                                </Border>

                            </WrapPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="tabExternalScripts" Header="External Scripts">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">

                        <TextBlock x:Name="txtExtScriptsWarning" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" FontStyle="Italic" Margin="0,0,0,10" FontSize="12"/>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="txtMassgraveTitle" Text="Windows &amp; Office Activation (MAS)" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtMassgraveDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                <Button x:Name="btnRunMassgrave" Content="Run Activation Script" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                            </StackPanel>
                        </Border>

                        <!-- UniGetUI -->
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="txtUniGetUITitle" Text="UniGetUI" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtUniGetUIDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                <Button x:Name="btnRunUniGetUI" Content="Open Official Website" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="txtOptiscalerExternalTitle" Text="Optiscaler Client" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtOptiscalerExternalDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                <Button x:Name="btnOpenOptiscalerClient" Content="Open Latest Release" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock x:Name="txtOcctExternalTitle" Text="OCCT" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtOcctExternalDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                <Button x:Name="btnOpenOcct" Content="Open Official Website" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="tabUtilities" Header="Utilities">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">

                        <GroupBox x:Name="grpUtilities" Header="System Restart Utilities">
                            <WrapPanel>
                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtSafeModeNetTitle" Text="Restart to Safe Mode with Networking" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtSafeModeNetDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnSafeModeNet" Content="Restart to Safe Mode with Networking" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtUefiRestartTitle" Text="Restart to UEFI Firmware Settings" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtUefiRestartDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnUefiRestart" Content="Restart to UEFI Firmware Settings" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtNormalRestartTitle" Text="Restart Normally" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtNormalRestartDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnNormalRestart" Content="Restart Normally" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpHiddenTools" Header="Hidden Windows Tools">
                            <WrapPanel>
                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtDiskCleanupTitle" Text="Disk Cleanup" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtDiskCleanupDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnDiskCleanup" Content="Open Disk Cleanup" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtRamCleanTitle" Text="Free Up RAM Now" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtRamCleanDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnRamClean" Content="Free Up RAM Now" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtResourceMonitorTitle" Text="Resource Monitor" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtResourceMonitorDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnResourceMonitor" Content="Open Resource Monitor" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtOptimizeDrivesTitle" Text="Optimize Drives (Defrag/TRIM)" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtOptimizeDrivesDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnOptimizeDrives" Content="Open Optimize Drives" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtMemDiagTitle" Text="Windows Memory Diagnostic" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtMemDiagDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnMemDiag" Content="Open Memory Diagnostic" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpSystemDiagnostics" Header="System Diagnostics &amp; Network Tools">
                            <WrapPanel>
                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtSystemIntegrityTitle" Text="System File Checker (SFC) &amp; DISM" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtSystemIntegrityDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnSystemIntegrity" Content="Run DISM &amp; SFC" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtFlushDNSTitle" Text="Flush DNS Cache" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtFlushDNSDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnFlushDNS" Content="Flush DNS Cache" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtReleaseRenewTitle" Text="Release / Renew IP" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtReleaseRenewDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnReleaseRenewIP" Content="Release / Renew IP" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtRegisterDNSTitle" Text="Register DNS" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtRegisterDNSDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnRegisterDNS" Content="Register DNS" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                        <GroupBox x:Name="grpAdvancedUtilities" Header="Updates, Diagnostics &amp; Automation">
                            <WrapPanel>
                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtSystemInfoTitle" Text="System Information" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtSystemInfoDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnSystemInfo" Content="Show System Info" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtStartupManagerTitle" Text="Startup Programs" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtStartupManagerDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnStartupManager" Content="Manage Startup Programs" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}" Width="300" Margin="0,0,10,10">
                                    <StackPanel>
                                        <TextBlock x:Name="txtScheduledOptTitle" Text="Scheduled Optimization" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock x:Name="txtScheduledOptDesc" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,10" FontSize="12"/>
                                        <Button x:Name="btnScheduledOptimization" Content="Create Scheduled Optimization" HorizontalAlignment="Left" Padding="16,8" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </GroupBox>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>